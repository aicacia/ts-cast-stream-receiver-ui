apiVersion: v1
kind: Service
metadata:
  name: {{ include "cast-stream-receiver-ui.fullname" . }}
  labels:
    {{- include "cast-stream-receiver-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cast-stream-receiver-ui.selectorLabels" . | nindent 4 }}
